import { CompetitionTypePipe } from './competition-type.pipe';

describe('CompetitionTypePipe', () => {
  it('create an instance', () => {
    const pipe = new CompetitionTypePipe();
    expect(pipe).toBeTruthy();
  });
});
