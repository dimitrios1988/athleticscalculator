import { MeetingCategoryPipe } from './meeting-category.pipe';

describe('MeetingCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new MeetingCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
